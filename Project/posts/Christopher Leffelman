Thursday September 19th 2013 8:07 pm -04:00,I am also getting the same errors listed above only am using the == instead of is operator.  Everything works fine in python.  Im not sure what I am doing wrong to get 20/24.  All of the examples come out fine.  Here is my code:def get_compliment(nucleotide):    (str) -> str    Returns the compliment to nucleotide.    >>> get_compliment(A)    T    >>> get_compliment(C)    G        if is_valid_sequence(nucleotide) != True:        return not a valid nucleotide    elif len(nucleotide) > 1:        return whoa there... just one nucleotide at a time please    elif nucleotide == A:        return T    elif nucleotide == T:        return A    elif nucleotide == C:        return G    elif nucleotide == G:        return C    elif len(nucleotide) == 0:        return no nucleotide entereddef get_complimentary_sequence(dna):    (str) -> str    Returns complimentary DNA sequence to string dna.    >>> get_complimentary_sequence(ATCG)    TAGC    >>> get_complimentary_sequence(GGGGGGG)    CCCCCCC        compseq =     if is_valid_sequence(dna) == True:        for nuc in dna:            comp = get_compliment(nuc)            compseq = compseq + comp    elif is_valid_sequence(dna) != True:        compseq = not a valid sequence    return compseq, Resolved,0
Thursday September 19th 2013 9:24 pm -04:00,Thank you so much!  I added all the extra code after thinking that was the problem and not that I was using the wrong word:), Resolved,0
