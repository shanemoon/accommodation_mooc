Tuesday August 20th 2013 8:48 am -04:00,import mathdef c_area(radius):     return pow(radius 2) * math.piradius = int(input(Please enter radius of circle: ))result = c_area(radius)print (Circle area is %s % (result))import is for importing library for getting pi number from computer Look for math.pi in code. pow and input are built in functions so they do not need extra library like math to be imported. Also int in int(input(...)) is for getting user input as integer.We can also check user input to be sure it is number but I think it needs higher level :),None,3
