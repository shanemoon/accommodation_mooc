Sunday September 22nd 2013 6:49 am -04:00,Dear all Im having some problems with Assignment 2 starting with get_length and is_longer. This is what I have so far:\>>> def get_length(dna):     (str) -> int    Return the length of the DNA sequence dna.    >>> get_length(ATCGAT)    6    >>> get_length(ATCG)    4    [Edit by AK CTA to remove working code]    def is_longer(dna1 dna2):     (str str) -> bool    Return True if and only if DNA sequence dna1 is longer than DNA sequence    dna2.    >>> is_longer(ATCG AT)    True    >>> is_longer(ATCG ATCGGA)    False    if get_length(dna1) > get_length(dna2)        return True      Using len(dna) seems to be the easiest way to get the number of characters. Both of them keep returning Errors (Syntax) when uploading. Can anyone give me a hint on this one?Thanks in advance.  Isabel, Resolved,0
Sunday September 22nd 2013 9:29 am -04:00,Thank you for your help! I now understand I wasnt working with codes. Now I tested only the first one (it has code/highlights) but I still get a syntax error. Is there something wrong with my code?>>> def get_length(dna):     (str) -> int    Return the length of the DNA sequence dna.    >>> get_length(ATCGAT)    6    >>> get_length(ATCG)    4        [Edit by AK CTA to remove working code] , Resolved,0
Sunday September 22nd 2013 9:46 am -04:00,Thanks! Below the error message:Message:ERROR: SyntaxErrorinvalid syntax(./a2.py 1 10 Python 3.3.2 (v3.3.2:d047928ae3f6 May 16 2013 00:03:43) [MSC v.1600 32 bit (Intel)] on win32), Resolved,0
Sunday September 22nd 2013 10:00 am -04:00,Thank you! Indeed I was working in the shell. I got some succesfull tests now so just need to work on my codes. Thanks again!, Resolved,0
